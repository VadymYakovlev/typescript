import { memo } from 'react';

const Contacts = memo(() => {
  return (
    <div>
      <div className='wrapper'>
        <h1>Contacts page</h1>
      </div>
    </div>
  );
});

export default Contacts;
